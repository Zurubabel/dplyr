x <- c(1,2,3,4)
y <- data.frame(x, c(8,4,5))
y <- data.frame(x, c(8,4,5, 4))
View(y)
knitr::opts_chunk$set(echo = TRUE)
summary(cars)
plot(pressure)
warning("Era essa a peça que você queria?")
warning("Era essa a peça que você queria?")
library(forecast)
install.packages("devtools")
library(forecast)
arima.sim
x <- qnorm(50)
ts_x <- ts(x)
plot(ts_x)
x <- qnorm(50)
ts_x <- ts(x)
ts.plot(ts_x)
plot(arima.sim(model = list(c(0,0,0))), n = 50)
plot(arima.sim(model = list(c(0,0,0)), n = 50))
plot(arima.sim(model = list(c(0,0,0)), n = 50))
plot(arima.sim(model = list(c(0,0,0)), n = 50))
plot(arima.sim(model = list(c(0,0,0)), n = 50))
#Criando o código
modelo_arima <- arima.sim(model = list(c(0,0,0)), n = 50)
plot(modelo_arima)
#Criando o código
modelo_arima <- arima.sim(model = list(c(0,0,0)), n = 50)
plot(modelo_arima)
#Criando o código
modelo_arima <- arima.sim(model = list(c(0,0,0)), n = 50)
plot(modelo_arima)
#Criando o código
modelo_arima <- arima.sim(model = list(c(0,0,0)), n = 50)
plot(modelo_arima)
#Criando o código
modelo_arima <- arima.sim(model = list(c(0,0,0)), n = 50)
plot(modelo_arima)
#Criando o código
modelo_arima <- arima.sim(model = list(c(0,0,0)), n = 50)
plot(modelo_arima)
#Criando o código
modelo_arima <- arima.sim(model = list(c(0,0,0)), n = 50)
plot(modelo_arima)
#Criando o código
modelo_arima <- arima.sim(model = list(c(0,0,0)), n = 50)
plot(modelo_arima)
#Criando o código
modelo_arima <- arima.sim(model = list(c(0,0,0)), n = 50)
plot(modelo_arima)
#Criando o código
modelo_arima <- arima.sim(model = list(c(0,0,0)), n = 50)
plot(modelo_arima)
#Criando o código
modelo_arima <- arima.sim(model = list(c(0,0,0)), n = 50)
plot(modelo_arima)
#Criando o código
modelo_arima <- arima.sim(model = list(c(0,0,0)), n = 50)
plot(modelo_arima)
#Carregando as bibliotecas necessárias
library(dplyr)
#Carregando as bibliotecas necessárias
library(dplyr)
1:10 %>% mean
#Carregando as bibliotecas necessárias
library(dplyr)
#Carregando as bibliotecas necessárias
library(dplyr)
#Carregando as bibliotecas necessárias
library(dplyr)
mtcars
mtcars[mtcars$mpg > 22]
mtcars$mpg
mtcars$mpg > 22
mtcars´[mtcars$mpg > 22]
mtcars[mtcars$mpg > 22]
mtcars$mpg[mtcars$mpg > 22]
mtcars[mtcars$mpg > 22]
mtcars$mpg[mtcars$mpg > 22]
subset(mtcars, mpg > 22)
mtcars %>% select(mpg > 20)
mtcars %>% filter(mpg > 20)
mtcars %>% filter(mpg > 20)
mtcars %>% filter(mpg > 20) %>% select(mpg, cyl)
mtcars %>% filter(mpg > 20) %>% select(mpg, cyl) %>% summarize(mean_mpg = mean(mpg))
ls()
subset(mtcars, mpg > 22)
subset(mtcars, mpg > 22)[1:3,]
subset(mtcars, mpg > 22)[,1:2]
mean(subset(mtcars, mpg > 22)[,1:2]$mpg)
media_mpg <- mean(subset(mtcars, mpg > 22)$mpg)
media_mpg <- mean(subset(mtcars, mpg > 22)$mpg)
mean(subset(mtcars, mpg > 22)$mpg)
mtcars %>% filter(mpg > 22) %>% summarize(media_mpg = mean(mpg))
mtcars %>% filter(mpg > 22) %>% summarize(mean(mpg))
mean(1:20)
# Operação sem o pipe
mean(1:20)
# Operação com o pipe
1:20 %>% mean
# Operação sem o pipe
mean(1:20)
# Operação com o pipe
1:20 %>% mean
1:24 %>% sd
mtcars
# Operação sem o pipe
mean(subset(mtcars, mpg > 22)$mpg)
# Operação com o pipe
# Operação sem o pipe
mean(subset(mtcars, mpg > 22)$mpg)
# Operação com o pipe
mtcars %>% subset(mpg > 22)
# Operação sem o pipe
mean(subset(mtcars, mpg > 22)$mpg)
# Operação com o pipe
mtcars %>% subset(mpg > 22)$mpg
# Operação sem o pipe
mean(subset(mtcars, mpg > 22)$mpg)
# Operação com o pipe
mtcars %>% subset(mpg > 22)
# Operação sem o pipe
mean(subset(mtcars, mpg > 22)$mpg)
# Operação com o pipe
mtcars %>% filter(mpg > 22) %>% summarize(mean(mpg))
# Operação sem o pipe
mean(subset(mtcars, mpg > 22)$mpg)
# Operação com o pipe
mtcars %>% filter(mpg > 22) %>% mean(mpg)
# Operação sem o pipe
mean(1:20)
# Operação com o pipe
# Operação sem o pipe
mean(1:20)
# Operação com o pipe
1:20 %>% mean
# Operação sem o pipe
mean(subset(mtcars, mpg > 22)$mpg)
# Operação com o pipe
# Operação sem o pipe
mean(subset(mtcars, mpg > 22)$mpg)
# Operação com o pipe
mtcars %>% filter(mpg > 22) %>% summarize(media_mpg = mean(mpg))
ggplot(mtcars, aes(mpg)) + gem_boxplot()
#Carregando as bibliotecas necessárias
library(dplyr)
library (ggplot2)
ggplot(mtcars, aes(mpg)) + gem_boxplot()
ggplot(mtcars, aes(mpg)) + geom_boxplot()
ggplot(mtcars, aes(mpg)) + geom_histogram()
# Operação sem o pipe
ggplot(mtcars, aes(mpg)) + geom_histogram()
# Operação com o pipe
mtcars %>% ggplot(aes(mpg)) + geom_histogram()
setwd("F:/videos/dplyr/aulas/Aula 2 - Percepção do dplyr")
#Importação da biblioteca dplyr
library(dplyr)
# Obtendo os dados do csv (NÃO ESQUEÇAM DE MUDAR O WORKSPACE USANDO A FUNÇÃO setwd OU NO MENU Session -> Set Working Directory)
tb_produtos <- read.csv("tabela.csv", header = TRUE)
View(tb_produtos)
#Importação da biblioteca dplyr
library(dplyr)
# Obtendo os dados do csv (NÃO ESQUEÇAM DE MUDAR O WORKSPACE USANDO A FUNÇÃO setwd OU NO MENU Session -> Set Working Directory)
tb_produtos <- read.csv("tabela.csv", header = TRUE, sep = ";")
View(tb_produtos)
#Importação da biblioteca dplyr
library(dplyr)
# Obtendo os dados do csv (NÃO ESQUEÇAM DE MUDAR O WORKSPACE USANDO A FUNÇÃO setwd OU NO MENU Session -> Set Working Directory)
tb_produtos <- read.csv("tabela.csv", header = TRUE,
sep = ";", encoding = "UTF-8")
View(tb_produtos)
#Importação da biblioteca dplyr
library(dplyr)
# Obtendo os dados do csv (NÃO ESQUEÇAM DE MUDAR O WORKSPACE USANDO A FUNÇÃO setwd OU NO MENU Session -> Set Working Directory)
tb_produtos <- read.csv("tabela.csv", header = TRUE,
sep = ";", encoding = "Latin-1")
View(tb_produtos)
#Importação da biblioteca dplyr
library(dplyr)
# Obtendo os dados do csv (NÃO ESQUEÇAM DE MUDAR O WORKSPACE USANDO A FUNÇÃO setwd OU NO MENU Session -> Set Working Directory)
tb_produtos <- read.csv("tabela.csv", header = TRUE,
sep = ";", encoding = "Latin-1")
View(tb_produtos)
#Importação da biblioteca dplyr
library(dplyr)
# Obtendo os dados do csv (NÃO ESQUEÇAM DE MUDAR O WORKSPACE USANDO A FUNÇÃO setwd OU NO MENU Session -> Set Working Directory)
tb_produtos <- read.csv("tabela.csv", header = TRUE,
sep = ";", encoding = "UTF-8")
View(tb_produtos)
1:20
# Operação sem o pipe
mean(1:20)
# Operação com o pipe
1:20 %>% mean
# Operação sem o pipe
mean(subset(mtcars, mpg > 22)$mpg)
# Operação com o pipe
#mtcars %>% filter(mpg > 22) %>% summarize(media_mpg = mean(mpg))
# Operação sem o pipe
mean(subset(mtcars, mpg > 22)$mpg)
# Operação com o pipe
mtcars %>% filter(mpg > 22) %>% summarize(media_mpg = mean(mpg))
# Operação sem o pipe
ggplot(mtcars, aes(mpg)) + geom_histogram()
# Operação com o pipe
#mtcars %>% ggplot(aes(mpg)) + geom_histogram()
# Operação sem o pipe
ggplot(mtcars, aes(mpg)) + geom_histogram()
# Operação com o pipe
mtcars %>% ggplot(aes(mpg)) + geom_histogram()
#Importação da biblioteca dplyr
library(dplyr)
# Obtendo os dados do csv (NÃO ESQUEÇAM DE MUDAR O WORKSPACE USANDO A FUNÇÃO setwd OU NO MENU Session -> Set Working Directory)
tb_produtos <- read.csv("tabela.csv", header = TRUE,
sep = ";", encoding = "UTF-8")
View(tb_produtos)
tb_produtos
View(tb_produtos)
View(tb_produtos)
tb_produtos %>% filter(X.U.FEFF.cdProduto > 10)
# tb_produtos %>% filter(X.U.FEFF.cdProduto > 10)
# Média
tb_produtos %>% filter(X.U.FEFF.cdProduto > 10) %>% summarize(mean(vlProduto))
# tb_produtos %>% filter(X.U.FEFF.cdProduto > 10)
# Média
tb_produtos %>% filter(X.U.FEFF.cdProduto > 10) %>% select(nmProduto) #%>% summarize(mean(vlProduto))
?select
setwd("F:/videos/dplyr/aulas/Aula 3 - Select parte 1")
mtcars %>% select(mpg)
mtcars %>% select(1:2)
mtcars %>% select(c(1,3,5))
mtcars %>% select(starts_with("m"))
mtcars
mtcars %>% select(mpg, cyl)
mtcars %>% select(c(mpg, cyl))
library(dplyr)
# Função select
select(mtcars, mpg)
# Função select
# select(mtcars, mpg)
mtcars %>% select(mpg)
mtcars]
mtcars
# Selecionando as colunas
mtcars %>%
select(mpg, cyl, disp)
# Selecionando as colunas
mtcars %>%
select(c(mpg, cyl, disp))
# Selecionando as colunas
# mtcars %>%
#select(mpg, cyl, disp)
#mtcars %>%
# select(c(mpg, cyl, disp))
mtcars %>%
select(1:4)
# Selecionando as colunas
# mtcars %>%
#select(mpg, cyl, disp)
#mtcars %>%
# select(c(mpg, cyl, disp))
mtcars %>%
select(c(1,3,5))
names(mtcars)
